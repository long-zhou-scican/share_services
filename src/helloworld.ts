export class HelloWorld {
    sayHello (name: string = 'World') {
        console.log(`Hello, ${name}`)
    }
    sayHello2 (name: string = 'World') {
        console.log(`Hello world, ${name}`)
    }
}